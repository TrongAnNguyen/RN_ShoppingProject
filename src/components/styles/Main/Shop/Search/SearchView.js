import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        paddingTop: 5
    },
    item: {
        marginVertical: 5,
        alignItems: 'center',
    }
});

export default styles;
